import { defineConfig } from 'vite';

export default defineConfig({
  server: {
    port: 3000,          // Puerto para tu servidor local
    open: true,           // Abre automáticamente en el navegador
  }
});
